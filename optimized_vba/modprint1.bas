Option Explicit

Dim NewPathAs String
ActiveWorkbook.SaveAs FileName:=NewPath & "\" & "whatever.xlsm", FileFormat:=xlOpenXMLWorkbookMacroEnabledo
Do WantTo Print Logsheet?
Folder path (Workbook folder + PDF)
Create folderif missing
If directory exist goto next line
Application.ActiveWorkbook.Path 'Default path name

Sub
Dimf
ExportAs FixedC` Txype
Next
n@,MsgBox
if miss
t Sub
If IsEmpty(strFilename) Then Exiscr/
ActiveWorkbook.SaveAs
End WithPasswbIf
